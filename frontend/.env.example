# Database
# For development (direct connection):
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"

# For production (use pooler for serverless):
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:6543/DATABASE?pgbouncer=true"

# JWT Secrets (CHANGE THESE IN PRODUCTION!)
JWT_ACCESS_TOKEN_SECRET="your-secret-key-min-32-chars"
JWT_REFRESH_TOKEN_SECRET="your-refresh-secret-key-min-32-chars"

# Supabase (optional - for file uploads)
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Application URL (optional - for password reset emails)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
